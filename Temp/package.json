{
  "name": "Temp",
  "version": "1.0.0",
  "description": "```\r $ curl -fsSL https://deb.nodesource.com/setup_17.x | sudo -E bash -\r $ sudo apt-get install -y nodejs\r ```\r ## Install awscli\r ### note this is v1.22 - you'd need to cmake git repo to get v2",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1030.0",
    "express": "^4.17.1",
    "ksuid": "^3.0.0",
    "node-dht-sensor": "^0.4.3"
  }
}
